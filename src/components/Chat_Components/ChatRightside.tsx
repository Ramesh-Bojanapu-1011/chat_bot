import React from 'react';
import { Card } from '../ui/card';

type Props = {};

const ChatRightside = (props: Props) => {
  return (
    <Card className="flex w-full     ">
      <div>jhg</div>
    </Card>
  );
};

export default ChatRightside;
